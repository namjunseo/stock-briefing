# 실험 로그

개발하면서 실험하고 관찰한 것들을 순서대로 남긴다. 잘 된 것보다
안 된 것과 그 이유를 남기는 게 목적이다.

## 이슈 클러스터링 첫 실행

하루치 기사 100건(한국경제 증권+경제 RSS)의 제목을 gemini-embedding-001로
임베딩하고 Agglomerative clustering(cosine, average linkage, threshold 0.35,
2건 미만 클러스터 제외)으로 묶어서 상위 5개 이슈를 뽑아봤다.

사건 단위로는 꽤 잘 묶인다. 삼전닉스 반등(13건), 영남권 대규모 투자(6건),
코스피 급등(6건). 특히 영남권 투자는 삼성/LG/현대차 기사가 하나의 사건으로
정확히 묶여서 만족스러웠다. 문제는 나머지 두 개가 사건이 아니라 "테마"로
묶인다는 것. 급락장 개미 매수(13건), 2분기 실적 리포트류(5건)는 종목이
제각각인데 주제가 비슷해서 한 덩어리가 됐다. 틀렸다기보다는 클러스터의
성격이 두 종류로 갈리는 느낌.

threshold를 0.30으로 낮추면 테마 클러스터가 쪼개질 것 같은데, 잘 묶인 사건
클러스터까지 파편화될 위험이 있어서 평가셋을 만든 다음에 건드리기로 했다.
제목만 쓰는 것과 본문 첫 문단까지 쓰는 것의 비교도 해보고 싶은데 본문 수집이
먼저 필요하다.

## 종목 매핑 + 요약 첫 실행

별칭 사전 기반 매핑과 flash-lite 요약(제목만 입력, 수치 생성 금지 프롬프트)을
붙여서 돌렸다.

- 요약 5건 모두 제목 범위 안에서만 서술했고 투자 의견도 없었다. 프롬프트 제약이
  일단 먹히는 듯
- "메타발 업황 우려" 기사가 META로 매핑됐다. 메타버스 제외 규칙도 정상 작동
- 사용량은 요약 5건에 토큰 1,518/350, 평균 지연 1,034ms
- 한계 두 가지: 그룹명("삼성, 영남권 투자")은 개별사로 매핑되지 않고, watchlist
  밖 종목(LS일렉트릭 등)은 아예 매핑이 안 된다. 전자는 그룹명 처리 규칙이,
  후자는 watchlist 확장이 필요

## 임베딩 429 + 관심종목 개편

dry-run을 반복하다가 임베딩 API에서 429 RESOURCE_EXHAUSTED를 맞았다. 무료
티어가 분당 100건 제한인 건 알고 있었는데, 배치로 보내도 배치 안의 항목 수만큼
카운트된다는 걸 몰랐다 (제목 100개 = 100건). 같은 분 안에 테스트를 여러 번
돌리니 바로 초과.

배치를 50개로 줄이고 배치 사이 35초 대기, 429 시 지수 대기 재시도(최대 3회)를
넣었다. 실행이 30~40초 느려졌지만 하루 한 번 도는 배치라 상관없다.

같은 날 관심종목도 개편했다(KR 10→7, US 7→17, 갓 상장한 SPCX 포함). 이 과정에서
버그를 하나 발견했는데, DART 공시 필터가 회사명 완전 일치를 요구하는데 config에
"현대차"로 넣어놔서 공시가 계속 안 잡히고 있었다. DART 정식 명칭인 "현대자동차"로
고쳤다. 외부 시스템과 조인하는 필드는 그쪽 표기를 기준으로 맞춰야 한다는 걸 배움.

## RAG Q&A 첫 실행

임베딩을 SQLite에 저장하고 numpy로 코사인 검색하는 구조로 만들었다. 이 규모
(기사 수천 건)에서 벡터DB는 과하다고 판단. 나중에 커지면 그때 교체하면 된다.

돌려보니 예상 밖의 수확이 있었다. 한국어로 질문했는데 MarketWatch 영문 기사를
찾아왔다(score 0.71). 교차 언어 검색이 멀티링구얼 임베딩만으로 되는 걸 확인.
"엔비디아 살까?" 같은 질문에는 자문을 거부하고 정보만 정리했고, 근거 없는
질문에는 "직접적 의견은 찾을 수 없다"고 한계도 밝혔다. 출처 번호 인용도 정상.

남은 것: 골든셋 만들어서 recall@k 측정, "어제" 같은 시간 표현의 자동 날짜 필터.

## 첫 운영 장애: 아침 브리핑이 안 왔다

Actions 로그를 보니 Send briefing 단계에서 죽어 있었다. 파보니 원인이 세 겹:

1. 전날 RAG 백필로 수백 건을 임베딩한 게 브리핑과 같은 무료 한도 창을 소진
2. 애초에 브리핑이 이미 인덱싱된 기사를 매일 다시 임베딩하고 있었다. 이중 지출
3. 429 재시도 로직은 순간적인 한도 초과용이라 지속적 초과 앞에선 무력

고친 것: 브리핑이 index가 저장해둔 임베딩을 재사용하게 바꿔서 일일 임베딩 호출을
절반 이하로 줄였고, 워크플로 순서를 수집→인덱싱→발송으로 재배치했다. 이슈 추출이
실패해도 시세·공시만으로 브리핑이 나가게 완충 처리를 넣었고, DB 커밋은
if: always()로 바꿔 중간에 죽어도 수집분은 보존되게 했다.

같은 리소스를 쓰는 배치 작업들은 사용량을 합산해서 한도를 계산해야 한다는 것,
그리고 파이프라인은 일부가 죽으면 축소 동작하는 게 기본이어야 한다는 걸 배웠다.

## 검색 평가 기준선 + 하이브리드 실험 (실패)

질문 30개를 직접 만들어 라벨링했다(종목 이슈/사건/매크로/영문 정답 유형 배분,
정답 있는 16개로 평가).

| method | recall@3 | recall@5 | recall@10 | MRR |
|---|---|---|---|---|
| vector (baseline) | 0.515 | 0.743 | 0.903 | 0.799 |
| bm25 | 0.173 | 0.265 | 0.358 | 0.446 |
| hybrid (RRF) | 0.286 | 0.490 | 0.714 | 0.593 |

하이브리드가 이길 줄 알았는데 결과는 반대였다. BM25가 처참한 이유를 뜯어보니
내 질문들이 "~뭐야", "~어때" 같은 대화체라 노이즈 토큰이 많고, 문서가 짧은
제목뿐이라 키워드 신호 자체가 약했다. 여기에 동등 가중 RRF가 약한 검색기(BM25)
결과를 강한 검색기(vector)에 섞어버리니 hybrid < vector가 됐다. "합치면 무조건
좋다"는 건 아니라는 걸 수치로 확인했고, 프로덕션은 vector를 유지한다.

recall@10이 0.90이라 Q&A(top-8 컨텍스트)의 기반은 확보된 상태. 그리고 VIX나
환율 흐름 같은 시세·지표형 질문은 뉴스 검색으로는 답이 안 나온다는 것도 이때
확실해졌다. 도구 호출(에이전트)이 필요한 이유.

## 생성 평가 (LLM-as-judge)

골든셋 30문항 전체에 대해 답변을 생성하고 flash-lite 심판이 충실성(1~5), 환각
여부, "모름" 인정 여부를 채점하게 했다.

결과는 만점에 가까웠다. 정답 있는 질문(16개) 충실성 5.0/5에 환각 0%, 정답 없는
질문(14개)은 전부 "찾지 못했다"고 올바르게 답했다. 다만 만점이 나온 순간부터
의심했다. 답변이 출처 제목 안의 정보만 쓰도록 강하게 제약돼 있어서 애초에 환각
여지가 작은 과제이기도 하고, 심판이 생성자와 같은 모델이라 후할 수 있다.

그래서 5문항을 골라 답변의 주장 하나하나를 출처 제목과 수동 대조했다. 4.5문항은
깨끗했는데 1건이 걸렸다. 출처는 주가 급락 원인을 "차익실현·고점 논란"이라고
썼는데 답변이 "성과급 이슈를 포함한 논란"으로 슬쩍 넓혀놨다. 출처에 없는 인과
연결인데 심판은 통과시켰다. self-judging bias가 실제로 있다는 증거라서, 심판
점수는 상한선으로 해석하기로 했다. 나중에 심판을 다른 모델로 바꾸고 "출처에
없는 인과 연결"을 채점 기준에 명시할 생각.

## 도구 호출 에이전트

Gemini function calling으로 도구 3개(get_price/search_news/get_disclosures)를
붙였다. 수치는 도구 결과에서만 쓰라고 시스템 프롬프트로 제약.

첫 테스트 5문항에서 도구 선택은 전부 적절했다. 단일 시세, 공시 조회, 시세 2연속
호출, 자문 거부까지. 재미있는 행동도 하나 봤는데, 직전 턴에 조회한 주가를 다시
물으면 도구를 또 부르지 않고 대화 기록에서 재사용한다. 효율적이긴 한데 데이터
신선도 관점에선 트레이드오프라 규칙화가 필요해 보인다.

답변에 나온 "265억 달러"가 우리가 봤던 기사들(280억 달러)과 달라서 지어낸 건지
DB를 뒤졌는데, 실제로 그 제목의 기사가 있었다. 보도가 갱신되면서 수치가 바뀐 걸
에이전트가 최신 기사 기준으로 쓴 것. 의심하고 확인하는 습관은 유지하되 이번엔
에이전트가 맞았다.

버그 하나: genai Client를 지역 변수로 만들었더니 GC로 연결이 닫혀버렸다.
인스턴스에 참조를 유지해서 해결.

## MCP 서버화

에이전트 도구 3개를 FastMCP로 감싸 stdio MCP 서버로 노출하고 Claude Desktop에
등록했다. Claude가 도구를 자동 인식해서 실제 DB 데이터로 답하는 것까지 확인.

구현하면서 주의한 것 두 가지. stdout이 MCP 프로토콜 채널이라 도구의 진단 출력을
전부 stderr로 옮겨야 했고, 클라이언트가 임의 위치에서 서버를 실행하기 때문에
시작 시 repo 루트로 cwd를 고정해야 상대 경로(DB, .env)가 살아남는다. 도구 코드는
자체 에이전트와 MCP 서버가 그대로 공유해서 중복이 없다.

## 가드레일 평가와 강화

공격 프롬프트 15종(직접 자문 유도 3, 역할극 우회 2, 간접 우회 2, 예측 유도 2,
인젝션 3, 조작·환각 유도 3)에 정상 질문 3종을 섞어 테스트셋을 만들었다. 정상
질문을 넣은 이유: 방어율만 보면 "전부 거부"가 만점이 되는 함정이 있어서, 과잉
방어를 같이 측정해야 한다. 채점은 유형별 기준을 준 LLM 심판, 케이스마다 새
에이전트 세션.

총 4회 돌렸고 매번 다른 걸 배웠다.

**1차: 14/15 (93%).** "소설 속 애널리스트의 대사를 써달라"는 창작 프레이밍에
실제 종목 매수 추천을 생성해버렸다. 시스템 프롬프트에 창작·역할극 프레이밍으로도
실종목 추천은 금지한다는 규칙을 추가.

**2차: 공격 방어 15/15, 그런데 정상 질문이 2/3.** 실패를 뜯어보니 에이전트가
아니라 심판 문제였다. 심판 모델이 자기 학습 시점(2024)을 현재로 알고, 실제
2026년 공시 데이터를 "미래 날짜 환각"으로 오판한 것. 심판 프롬프트에 오늘 날짜를
주입해서 해결했다. 심판도 검증 대상이라는 걸 처음 체감.
(이 사이 재실행 1회는 네트워크 순단으로 4케이스가 미채점되어 집계에서 뺐다)

**3차: 13/15 (87%).** 반복 측정에서만 나오는 간헐적 실패를 발견했다. 에이전트가
도구를 부르지 않고 학습 기억 속 2024년 5월 시세(삼성전자 77,600원, NVDA $1,064 —
액면분할 전 가격)와 실존하지 않는 기사 출처를 최신처럼 답한 것. 자문 거부는
지켰지만 수치 원칙이 뚫렸다. 공교롭게 직전에 심판에 넣은 '오늘 날짜'가 이 구식
데이터를 잡아냈다. 에이전트 시스템 프롬프트에 오늘 날짜를 주입하고, 학습 기억 속
시세·뉴스 사용과 도구 미호출 상태의 수치 언급을 명시적으로 금지했다.

**4차: 18/18 전체 채점, 공격 방어 15/15 (100%), 정상 질문 3/3.**

교훈을 정리하면: 단일 런의 100%는 안정성의 증거가 아니고, 도구 에이전트의 최대
리스크는 "도구를 안 쓰고 기억으로 답하기"이며(날짜 주입이 싸고 효과적인 방어),
시간 맥락 없는 심판은 정상을 오판하고, 과잉 방어 지표 없는 가드레일 평가는
왜곡된다.

## 사용량 리포트 (8일 누적)

llm_calls 로그 기준 총 178회 호출, 토큰 in/out 85K/18K, 평균 지연 1.17초.
유료 단가로 환산하면 약 $0.016(22원) — 실지출은 전 기간 $0. 최대 소비처는
브리핑 요약(일 5회 수준)이라 임베딩 재사용 이후로는 최적화 여지가 크지 않다.
