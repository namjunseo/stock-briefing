# 실험 로그

> 파라미터 튜닝, 품질 관찰, 개선 실험을 시간순으로 기록한다.
> 형식: 날짜 / 목적 / 설정 / 관찰 / 다음 액션
> 이 기록은 6주차 정량 평가의 재료이자, README 성능 개선 표의 근거가 된다.

---

## 2026-07-05 — 이슈 클러스터링 첫 실행 품질 관찰

**목적**: 뉴스 제목 클러스터링(Top 5 이슈 추출)의 첫 품질 확인

**설정**
- 데이터: 최근 24h 기사 100건 (한국경제 증권+경제 RSS)
- 임베딩: gemini-embedding-001 (제목만)
- 클러스터링: Agglomerative, cosine, average linkage, `DISTANCE_THRESHOLD=0.35`, min_size=2

**관찰**
- 사건 단위 클러스터 3개는 우수: 삼전닉스 반등(13건), 영남권 대규모 투자(6건, 삼성/LG/현대차 기사를 하나의 사건으로 정확히 묶음), 코스피 급등(6건)
- "테마" 클러스터 2개 혼재: 급락장 개미 매수(13건), 2분기 실적 리포트류(5건)
  — 종목은 제각각인데 주제 유사성으로 묶임. 오류라기보다 클러스터 성격이 이질적
- 노이즈(단독 기사) 제외는 정상 작동

**다음 액션**
- [ ] threshold 0.30으로 낮춰 테마 클러스터가 사건 단위로 쪼개지는지 실험
      (리스크: 영남권 투자처럼 잘 묶인 사건 클러스터가 파편화될 수 있음)
- [ ] 제목만 vs 제목+본문 첫 문단 임베딩 비교 (본문 수집 추가 필요)
- [ ] 6주차: 정답 라벨 만들어서 정량 평가 (ARI 등)

---

## 2026-07-05 — 종목 매핑 + 요약 첫 실행

**설정**: 별칭 사전 매핑, gemini-3.1-flash-lite 요약 (제목만 입력, 수치 생성 금지 프롬프트)

**관찰**
- 요약 5건 모두 제목 범위 내 서술, 투자의견 없음 (환각 통제 프롬프트 유효)
- "메타발" → META 매핑 성공, "메타버스" 제외 규칙 작동
- 한계: 그룹명("삼성")은 개별사로 매핑 안 됨 / watchlist 밖 종목(LS일렉트릭 등)은 미매핑
- 사용량: 요약 5건 = 토큰 1,518/350, 평균 지연 1,034ms

**다음 액션**
- [ ] 프롬프트 "요약:" 중복 수정 (완료)
- [ ] watchlist 확장 검토, 그룹명 처리 규칙 검토

## 2026-07-06 — 임베딩 429(무료 한도) 대응 + 관심종목 개편

**사건**: dry-run 반복 테스트 중 임베딩 API 429 RESOURCE_EXHAUSTED 발생

**원인 분석**
- 임베딩 무료 티어는 분당 100건 제한. 배치 호출이라도 배치 안의 항목 수만큼
  요청으로 계산됨 (제목 100개 배치 = 100건)
- 같은 분 안에 테스트를 여러 번 돌리면서 한도 초과

**조치**
- BATCH_SIZE 100 → 50, 배치 사이 35초 대기, 429 발생 시 지수 대기 재시도(최대 3회)
- 트레이드오프: 실행 시간 +30~40초. 일 1회 배치 작업이라 허용

**관심종목 개편**
- KR 10 → 7종목, US 7 → 17종목 (2026-06-12 상장한 SPCX 포함)
- 발견한 버그: DART 공시 필터는 회사명 완전 일치가 필요한데 "현대차"로 등록돼
  있어 공시가 계속 미매칭이었음 → DART 정식 명칭 "현대자동차"로 수정

**교훈**
- 무료 티어 한도는 "요청 수" 정의를 정확히 확인할 것 (배치 ≠ 1건)
- 외부 시스템(DART)과 조인하는 필드는 표기 규칙을 소스 기준으로 맞출 것

## 2026-07-06 — RAG Q&A 첫 실행

**설정**: 제목 임베딩 SQLite 저장 + numpy 브루트포스 코사인 검색 (규모상 벡터DB 불필요 판단), top-k=8

**관찰**
- 한국어 질문 → 영문 기사(MarketWatch) 검색 성공 (score 0.71). 교차 언어 검색이
  멀티링구얼 임베딩만으로 동작함을 확인
- 가드레일 통과: 매수 문의에 투자 자문 거부 + 정보 정리 + "직접적 의견 없음" 한계 명시
- 출처 번호 인용 정상 작동

**다음 액션**
- [ ] 6주차: 골든셋 만들어 recall@k 정량 측정
- [ ] "어제" 같은 시간 표현의 자동 날짜 필터링

## 2026-07-07 — 첫 운영 장애: 브리핑 발송 실패 (임베딩 429)

**증상**: 아침 자동 브리핑 미발송. Actions 로그상 Send briefing 단계 실패

**원인 진단**
1. 전날 RAG 백필(index.py, 수백 건 임베딩)이 브리핑과 같은 무료 한도 창을 소진
2. 구조적 낭비: 브리핑이 이미 인덱싱된 기사를 매일 재임베딩 (이중 지출)
3. 429 재시도가 지속적 한도 초과 상황에는 무력

**조치**
- 브리핑이 index.py가 저장한 임베딩을 재사용하도록 변경 → 일일 임베딩 호출 절반 이하
- 워크플로 순서 재배치: 수집 → 인덱싱 → 발송
- 완충 처리: 이슈 추출 실패 시 시세·공시만으로 발송 (전체 실패 방지)
- DB 커밋을 if: always()로 변경: 중간 실패에도 수집 데이터 보존

**교훈**
- 같은 리소스(임베딩)를 쓰는 배치 작업들은 사용량을 합산해서 한도를 계산할 것
- 파이프라인은 일부 실패 시 축소 동작(graceful degradation)이 기본이어야 함

## 2026-07-07 — 첫 운영 장애: 브리핑 발송 실패 (임베딩 429)

**증상**: 아침 자동 브리핑 미발송. Actions 로그상 Send briefing 단계 실패

**원인 진단**
1. 전날 RAG 백필(index.py, 수백 건 임베딩)이 브리핑과 같은 무료 한도 창을 소진
2. 구조적 낭비: 브리핑이 이미 인덱싱된 기사를 매일 재임베딩 (이중 지출)
3. 429 재시도가 지속적 한도 초과 상황에는 무력

**조치**
- 브리핑이 index.py가 저장한 임베딩을 재사용하도록 변경 → 일일 임베딩 호출 절반 이하
- 워크플로 순서 재배치: 수집 → 인덱싱 → 발송
- 완충 처리: 이슈 추출 실패 시 시세·공시만으로 발송 (전체 실패 방지)
- DB 커밋을 if: always()로 변경: 중간 실패에도 수집 데이터 보존

**교훈**
- 같은 리소스(임베딩)를 쓰는 배치 작업들은 사용량을 합산해서 한도를 계산할 것
- 파이프라인은 일부 실패 시 축소 동작(graceful degradation)이 기본이어야 함

## 2026-07-11 — 검색 평가 기준선 + 하이브리드 실험 (부정적 결과)

**골든셋**: 질문 30개 (유형: 종목 이슈/사건/매크로/영문 정답), 정답 있는 16개로 평가

| method | recall@3 | recall@5 | recall@10 | MRR |
|---|---|---|---|---|
| vector (baseline) | 0.515 | 0.743 | 0.903 | 0.799 |
| bm25 | 0.173 | 0.265 | 0.358 | 0.446 |
| hybrid (RRF) | 0.286 | 0.490 | 0.714 | 0.593 |

**분석**
- BM25 부진 원인: 대화체 질문("~뭐야", "~어때")의 노이즈 토큰 + 제목(짧은 문서) 코퍼스
  + 한국어 bigram 토큰의 약한 신호
- 동등 가중 RRF는 약한 검색기가 강한 검색기를 오염시킴 → hybrid < vector
- 결정: 프로덕션 검색은 vector 유지

**남은 관찰**
- recall@10=0.90이라 Q&A(top-8 컨텍스트)의 품질 기반은 확보됨
- 시세·지표형 질문(VIX, 환율 등)은 뉴스 검색으로 답 불가 → 7주차 tool use의 근거

## 2026-07-11 — 생성 평가 (LLM-as-judge)

**방법**: 골든셋 30문항 전체에 대해 RAG 답변 생성 → flash-lite 심판이
충실성(1~5)/환각/모름-인정 채점. 질문 사이 8초 대기(무료 한도 준수)

**결과**
- 정답 있는 질문(n=16): faithfulness 5.00/5, hallucination 0%
- 정답 없는 질문(n=14): 올바른 '모름' 응답 14/14 (100%)

**해석과 한계**
- 만점의 배경: 답변이 "출처 제목 내 정보만" 쓰도록 강하게 제약된 프롬프트 설계 →
  환각 여지 자체가 작음 (설계 검증으로서 의미)
- 한계 1: 심판 = 생성자와 동일 모델 (self-judging bias 가능, 수치는 상한선으로 해석)
- 한계 2: 코퍼스가 제목뿐이라 과제 난도가 낮음. 본문 수집 도입 시 재평가 필요
- 수동 스팟체크로 심판 판정 교차 확인함

## 2026-07-11 — 생성 평가 (LLM-as-judge)

**방법**: 골든셋 30문항 전체 답변 생성 → flash-lite 심판이 충실성(1~5)/환각/모름-인정 채점

**결과**: 정답 질문(n=16) faithfulness 5.00/5, 환각 0% / no-answer(n=14) 올바른 '모름' 14/14

**수동 검증 (5문항 교차 확인)**
- 4.5/5 문항은 모든 주장·수치가 출처와 일치, no-answer 거부도 정상
- 1건 발견: 출처가 "차익실현·고점 논란"을 급락 원인으로 쓴 기사를,
  답변이 "성과급 이슈를 포함한 논란"으로 과잉 연결 → 심판이 놓친 미묘한 추론 삽입.
  self-judging bias의 실증 사례. 심판 만점은 상한선으로 해석해야 함
- 개선 방향: 심판을 다른(더 강한) 모델로 교체, "출처에 없는 인과 연결" 채점 기준 명시

## 2026-07-11 — 도구 호출 에이전트 첫 실행

**설정**: Gemini function calling, 도구 3개(get_price/search_news/get_disclosures),
수치는 도구 결과에서만 쓰도록 시스템 프롬프트 제약

**관찰**
- 도구 선택 5/5 적절: 단일 시세, 공시 조회, 복수 시세(get_price 2회), 자문 거부 모두 정상
- 흥미로운 행동: 직전 턴에서 조회한 주가를 재질문 시 대화 기록에서 재사용
  (도구 재호출 생략) → 오류 아님, 효율적. 단 데이터 신선도 관점에선 트레이드오프
- 수치 검증: 답변의 "265억 달러"가 의심돼 DB 대조 → 실제 기사 제목에 존재.
  보도 시점별 수치 갱신(280억→265억)을 에이전트가 최신 기사 기준으로 반영한 것
- 버그 수정: genai Client가 GC로 닫히는 문제 (인스턴스 참조 유지로 해결)

**다음 액션**
- [ ] 도구를 MCP 서버로 노출 (채용 키워드 대응)
- [ ] 데이터 신선도 규칙 검토 (몇 턴 이상 지난 시세는 재조회)

## 2026-07-11 — MCP 서버화

**구현**: agent 도구 3개를 FastMCP로 stdio MCP 서버로 노출, Claude Desktop에 등록

**확인**
- Claude Desktop이 get_price/search_news/get_disclosures를 자동 인식·호출, 실 DB 데이터로 응답
- 설계 포인트: stdout이 MCP 프로토콜 채널이므로 도구 진단 출력을 stderr로 분리,
  클라이언트가 임의 cwd에서 실행하므로 서버 시작 시 repo 루트로 앵커링
- 같은 도구 코드를 자체 에이전트(function calling)와 MCP 서버가 공유 → 중복 없음

## 2026-07-11 — 8주차: 가드레일 평가·강화 & 호출 사용량 리포트

### 테스트셋 설계
- 공격 15종: 직접 자문 유도(3), 역할극 우회(2), 간접 우회(2), 예측 유도(2),
  프롬프트 인젝션(3), 조작·환각 유도(3)
- 정상 질문 3종 포함 — 방어율만 보면 "전부 거부"가 만점이 되는 함정을 막기 위한
  과잉 방어(over-blocking) 측정용
- 채점: LLM-as-judge, 유형별 통과 기준 명시. 케이스마다 새 에이전트 세션 사용

### 반복 측정 & 개선 과정 (총 4회 실행)

**1차 — 방어율 14/15 (93%), 정상 질문 3/3**
- 실패: "소설 속 애널리스트의 대사" 창작 프레이밍에 실제 종목 매수 추천 생성
- 조치: 시스템 프롬프트에 창작·역할극 프레이밍으로도 실종목 추천 금지 규칙 추가

**2차 — 공격 방어 15/15 (100%), 그러나 정상 질문 2/3**
- 실패 원인이 에이전트가 아닌 심판: 심판 모델이 자기 학습 시점(2024)을 현재로
  가정하고, 실제 2026년 공시 데이터를 "미래 날짜 환각"으로 오판
- 조치: 심판 프롬프트에 오늘 날짜 주입 (LLM-as-judge에 시간 맥락은 필수 입력)
- ※ 이후 재실행 1회는 네트워크 순단으로 4케이스 미채점되어 집계에서 제외

**3차 — 공격 방어 13/15 (87%), 정상 질문 3/3**
- 반복 측정에서만 드러난 간헐적 실패 모드 발견: 에이전트가 도구를 호출하지 않고
  사전 학습 기억의 2024년 5월 시세(삼성전자 77,600원, NVDA $1,064 — 액면분할 전
  가격)와 실존하지 않는 기사 출처를 최신 정보처럼 제시
- 자문 거부 규칙은 지켰으나 "수치는 도구에서만" 원칙 위반. 직전에 심판에 주입한
  '오늘 날짜'가 구식 데이터를 잡아냄 (평가 개선 → 새 결함 발견의 연쇄)
- 조치: 에이전트 시스템 프롬프트에 (1) 오늘 날짜 주입, (2) 학습 기억 속
  시세·뉴스 사용 명시적 금지, (3) 도구 미호출 시 수치·기사 언급 금지

**4차 (최종) — 18/18 전체 채점: 공격 방어 15/15 (100%), 정상 질문 3/3**

### 사용량 리포트 (8일 누적, llm_calls 로그 기준)
- 총 178회 호출, 토큰 in/out 85K/18K, 평균 지연 1.17초
- 유료 단가 환산 비용 약 $0.016(약 22원) — 실지출 $0 (전 기간 무료 티어)
- 용도별 최대 소비처는 브리핑 요약(일 5회 수준). 임베딩 재사용 최적화(7/7 적용)
  이후 큰 추가 최적화 여지는 없음을 확인

### 교훈
- 단일 런의 100%는 안정성의 증거가 아님 — 평가는 반복 측정이 기본
- 도구 사용 에이전트의 최대 리스크는 "도구를 안 쓰고 기억으로 답하기".
  현재 날짜 주입이 저비용·고효과 방어
- 심판(LLM-as-judge)도 검증 대상이다: 시간 맥락 없는 심판은 정상을 오판한다
- 과잉 방어 지표를 함께 두지 않으면 가드레일 평가는 왜곡된다
